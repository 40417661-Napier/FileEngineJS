/*

Create a static array (of size 50) and read in a name from the user. Based on the size of the name the user enters, create a character 
array on the heap using malloc to store the name. (use the strcpy function to copy the name from the stack array to the heap)
The program should loop this last point so it can dynamically store different length names.
Note you will need to free before you malloc again to avoid a memory leak.

*/

#include <stdio.h>
#include <stdlib.h>
#include "./myfunctions.h"

int main(int argc, char **argv)
{
    /* code */
    return 0;
}
